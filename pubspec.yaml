name: clinic_data_collector
description: تطبيق جمع بيانات العيادات والمراكز الطبية
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  supabase_flutter: ^2.10.0
  flutter_riverpod: ^2.6.1
  go_router: ^16.2.0
  image_picker: ^1.1.2
  geolocator: ^14.0.2
  permission_handler: ^12.0.1
  google_fonts: ^6.3.0
  intl: ^0.20.2
  uuid: ^4.5.1
  csv: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
